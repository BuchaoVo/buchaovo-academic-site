# Security

Do not report private credentials or security-sensitive information in a public issue.

This is a static website and should not contain secrets. Never commit API keys, private tokens, SSH keys, cloud credentials, private datasets, or unpublished confidential material. Use GitHub repository secrets only if a future workflow genuinely requires credentials.
